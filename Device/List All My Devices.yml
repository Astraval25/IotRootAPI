info:
  name: List All My Devices
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/api/devices"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
