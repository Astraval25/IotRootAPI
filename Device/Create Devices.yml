info:
  name: Create Devices
  type: http
  seq: 3

http:
  method: POST
  url: "{{host}}/api/devices"
  body:
    type: json
    data: |
      {
        "username": "node_1",
        "password": "1234567890",
        "mountpoint": ""
      }
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
