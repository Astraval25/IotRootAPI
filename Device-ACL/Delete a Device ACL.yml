info:
  name: Delete a Device ACL
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{host}}/api/devices/:deviceId/rules/:rulesId"
  params:
    - name: rulesId
      value: ""
      type: path
    - name: deviceId
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{adminToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
