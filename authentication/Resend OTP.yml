info:
  name: Resend OTP
  type: http
  seq: 5

http:
  method: POST
  url: "{{host}}/api/auth/resend-otp"
  body:
    type: json
    data: |-
      {
        "email":"admin@astraval.com"
      }
  auth:
    type: bearer
    token: "{{adminToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
