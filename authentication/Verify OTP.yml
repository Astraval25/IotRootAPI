info:
  name: Verify OTP
  type: http
  seq: 3

http:
  method: POST
  url: "{{host}}/api/auth/verify-otp"
  body:
    type: json
    data: |-
      {
        "email":"karthikeyan.aas@gmail.com",
        "otp":"158508"
      }
  auth:
    type: bearer
    token: "{{adminToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
