info:
  name: Register
  type: http
  seq: 2

http:
  method: POST
  url: "{{host}}/api/auth/register"
  body:
    type: json
    data: |-
      {
        "email":"karthikeyan.aas@gmail.com",
        "password":"Admin@123"
      }
  auth:
    type: bearer
    token: "{{adminToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
