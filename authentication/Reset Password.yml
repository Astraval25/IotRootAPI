info:
  name: Reset Password
  type: http
  seq: 7

http:
  method: POST
  url: "{{host}}/api/auth/reset-password"
  body:
    type: json
    data: |-
      {
        "email":"admin@astraval.com",
        "otp":"",
        "newPassword":"",
      }
  auth:
    type: bearer
    token: "{{adminToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
