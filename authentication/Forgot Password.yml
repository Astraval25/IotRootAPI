info:
  name: Forgot Password
  type: http
  seq: 6

http:
  method: POST
  url: "{{host}}/api/auth/forgot-password"
  body:
    type: json
    data: |-
      {
        "email":"admin@astraval.com"
      }
  auth:
    type: bearer
    token: "{{adminToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
